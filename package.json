{
  "name": "ts-barebones",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "automate": "tsc && node out/main.js",
    "dry-run": "tsc && node out/main.js --dry-run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MM",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^17.0.38"
  }
}
